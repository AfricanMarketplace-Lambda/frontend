//import {actions} from '../actions';

export const initialState = {
    // different states here 
}

export const reducer = ( state = initialState, action) => {
    switch (action.type){
        // different action cases here
        default: 
        state
    }
}